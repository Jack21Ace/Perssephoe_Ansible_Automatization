---
all:
  hosts:
    localhost:
      ansible_connection: local

    WebServerA1:
      ansible_host: 192.168.1.11
      ansible_user: nginx       # Reemplaza con el usuario que configuraste en SSH
      ansible_ssh_private_key_file: ~/.ssh/id_rsa  # Ruta a la clave privada en la máquina host
